package dev.bullrich;

public class Exercise8Arrays {
    public static void main(String[] args) {
        // having the following array
        String[] words;

        // ask the user for a number and create and array of that size

        // then in a for loop of the size of the array, get the user input and add every word to a new index of the array

        // at the end, print all the words separated by a dash
    }
}
